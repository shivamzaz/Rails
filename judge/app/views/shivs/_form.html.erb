<%= form_for(@shiv) do |f| %>
  <% if @shiv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shiv.errors.count, "error") %> prohibited this shiv from being saved:</h2>

      <ul>
      <% @shiv.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
