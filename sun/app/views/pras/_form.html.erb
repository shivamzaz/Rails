<%= form_for(@pra) do |f| %>
  <% if @pra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pra.errors.count, "error") %> prohibited this pra from being saved:</h2>

      <ul>
      <% @pra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_area :color %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
